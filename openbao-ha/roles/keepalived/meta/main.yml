---
# ⚠️ DEPRECATED — ce rôle n'est plus utilisé depuis le passage à la
# bascule Floating IP OpenStack via l'API Neutron (rôle openstack_fip).
# Conservé pour référence / rollback éventuel. Ne plus brancher dans site.yml.
galaxy_info:
  role_name: keepalived
  namespace: acme
  author: Acme Corp Platform Engineering
  description: "[DEPRECATED] keepalived (VRRP) — remplacé par openstack_fip (Neutron Floating IP)."
  company: Acme Corp
  license: Apache-2.0
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - keepalived
    - vrrp
    - vip
    - ha

dependencies: []
